# 2026-03-11 记忆日志

## Antalpha RWA 技能修复

### 问题修复记录

**问题1：EIP-681 支付链接格式错误**
- 修复：将原生 ETH 转账格式改为 USDT ERC-20 transfer 格式
- USDT 合约地址：0x833589fcd6edb6e08f4c7c32d4f71b54bda02913
- 正确格式：`ethereum:{USDT_CONTRACT}@{chain_id}/transfer?address={to}&uint256={amount}`

**问题2：config/mcp.json 未被读取**
- 修复：添加 `load_config()` 函数，优先读取配置文件
- MCP_URL 从硬编码改为从配置读取

**问题3：SKILL.md 产品描述**
- 大幅精简：从 157 行减至 27 行
- 纠正描述：底层资产是 BTC-backed（BTC质押借贷），投资货币是 USDT
- 移除所有硬编码产品参数，强调从服务器实时获取

### 经验教训

**错误原因反思：**
- 复制 eth-payment 技能代码时"断章取义"，只复制了原生代币部分
- eth-payment 有 `is_native` 判断逻辑，分别处理原生代币和 ERC-20
- 未完整理解代码逻辑导致错误

**SKILL.md 编写规范：**
- 少即是多：文档应该精简为技能使用说明
- 不能将 SKILL.md 作为数据源
- 产品参数（期限、利率）必须从服务器实时读取
- 全英文，不允许中英混杂

### 相关文件
- `skills/antalpha-rwa/scripts/rwa_client.py`
- `skills/antalpha-rwa/SKILL.md`
- GitHub: https://github.com/AntalphaRWA/antalpha-rwa-skill

检索标签：#技能修复 #EIP-681 #SKILL.md #少即是多

---

## GitHub 验证与澄清

### 用户疑问
用户反馈 GitHub 上 SKILL.md 仍是 206 行，怀疑未推送成功。

### 实际验证
- 本地 SKILL.md：99 行
- GitHub SKILL.md：99 行（通过 curl 验证）
- 最新提交：`0a52424`

### 结论
用户可能看到浏览器缓存的旧版本。实际已推送成功。

### 三个问题最终状态
| 问题 | 状态 | 验证方式 |
|------|------|---------|
| SKILL.md 精简 | ✅ 已修复 | curl 验证 99 行 |
| MCP_URL 硬编码 | ✅ 已修复 | 代码中从 config 读取 |
| EIP-681 格式 | ✅ 已修复 | ERC-20 transfer 格式正确 |

### 关键代码验证
```python
# EIP-681 正确格式
USDT_CONTRACT = "0x833589fcd6edb6e08f4c7c32d4f71b54bda02913"
eip681_link = f"ethereum:{USDT_CONTRACT}@{chain_id}/transfer?address={receiving_address}&uint256={amount_raw}"

# MCP_URL 从配置读取
_CONFIG = load_config()
MCP_URL = _CONFIG["mcp_url"]
```

检索标签：#GitHub验证 #缓存问题 #代码审查

---

## GitHub 同步问题调查

### 用户反馈
用户确认强制刷新后，GitHub 上仍是老版本（206 行）。

### 调查结果
| 验证方式 | 行数 | 字节数 | 状态 |
|---------|------|--------|------|
| 本地文件 | 99 | 3405 | ✅ 正确 |
| GitHub (curl) | 99 | 3405 | ✅ 正确 |
| GitHub API | - | 3405 | ✅ 正确 |
| 最新提交 | 0a52424 | - | ✅ 正确 |

### 结论
所有技术验证都显示 GitHub 上是正确版本。可能原因：
1. 用户访问了错误的仓库
2. 企业 GitHub 有额外的缓存层
3. 需要检查具体的 URL

### 下一步
请用户确认访问的 URL 是否为：
`https://github.com/AntalphaRWA/antalpha-rwa-skill`

检索标签：#GitHub同步 #技术验证 #问题排查