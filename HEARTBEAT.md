- 检查来自 Derar Islim (derar.islim@antalpha.com) 的新邮件并通知。

**注意**：邮件审批分析由 Crontab 定时任务处理（每天 10:00、15:00、20:00），心跳不再重复执行。

## 记忆维护任务（每周执行）
检查 `memory/heartbeat-state.json` 中的 `lastMemoryMaintenance` 字段。
如果距今超过 7 天，执行以下维护流程：
1. 读取最近 7 天的日志文件 `memory/YYYY-MM-DD.md`
2. 提炼有长期价值的信息，归档到对应文件：
  - 项目决策和状态 → `memory/projects.md`
  - 问题解决方案 → `memory/lessons.md`
3. 压缩已完成的一次性任务为一行总结
4. 删除完全过期的临时信息
5. 更新 `heartbeat-state.json` 中的 `lastMemoryMaintenance` 为当前日期

**注意**：Dream Cycle（每天凌晨 3:00 Cron）已自动处理实体扫描、反向链接修复、格式检查和记忆压缩。心跳中的记忆维护主要处理**实时对话后的即时更新**。

## Brain 实体图谱维护

**触发时机：** 每次心跳检查时，检查是否有需要更新的实体。

**检查流程：**
1. 检查 `brain/` 目录是否存在于会话中
2. 回顾最近对话，识别是否提到了新的实体或现有实体的新信息
3. 如果发现新实体或需要更新的信息：
   - 新实体 → 创建对应的 .md 文件
   - 现有实体 → 更新「时间线」或「笔记」
   - 更新「最后更新」时间戳
4. 更新 `brain/INDEX.md` 中的实体索引表

**写入原则：**
- 只写入 **明确的、有价值的信息**（决策、事件、偏好变化）
- 闲聊、情绪表达不写入
- 保持交叉引用格式 `[[实体名]]`
- 同步更新 `brain/INDEX.md` 索引

## 每周宏观分析报告
- **触发时间**: 每周一上午 9:00 (Asia/Shanghai)。
- **执行流程**:
  1. 检查当前是否为周一上午。
  2. 读取 `reports/` 目录下最新的分析报告。
  3. 遵循 `AGENTS.md` 中定义的“黄金标准”分析方法，搜集所有最新数据。
  4. 进行差异化分析，生成一份聚焦于“新洞察”的报告。
  5. 主动将新报告发送给你。